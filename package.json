{
  "name": "crypto-backend",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest",
    "db": "npx prisma generate && npx prisma migrate dev --name init && yarn ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@fastify/caching": "^8.4.0",
    "@prisma/client": "^6.15.0",
    "fastify": "^4.24.3",
    "pino-pretty": "^10.2.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "^20.8.0",
    "dotenv": "^17.2.1",
    "jest": "^29.7.0",
    "prisma": "^6.15.0",
    "ts-jest": "^29.1.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.0"
  }
}
